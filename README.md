# web-Q-A
This is a web app where users enter a URL and ask questions based only on that page’s content. It reads the webpage, finds the main text, and shows the most relevant answer using smart search (no Google used). It uses Streamlit, NLP, and vector search to find answers fast.
